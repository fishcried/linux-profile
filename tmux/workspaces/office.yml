name: office
root: ~/

windows:
  - timer: timer
  - editor: pwd
  - logs:
      layout: tiled
      root: ~/Projects/fishcried.github.io
      panes:
        - vim:
          - vim
        - server:
          - bundle exec jekyll server --watch -D -H 0.0.0.0 -P 4000
